all:
  children:
    vpn_servers:
      hosts:
        vpn-1.example.com:
          ansible_host: 203.0.113.10
          ansible_user: root
          ansible_port: 22
    proxy_servers:
      hosts:
        proxy-1.example.com:
          ansible_host: 198.51.100.20
          ansible_user: root
          ansible_port: 22
