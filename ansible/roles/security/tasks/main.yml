---
- name: Build effective SSH and firewall port sets
  ansible.builtin.set_fact:
    security_ssh_effective_ports: [22]
    security_firewall_effective_ssh_ports: [22]

- name: Build effective public HTTP/HTTPS toggles
  ansible.builtin.set_fact:
    security_firewall_effective_http: "{{ ovpn_firewall_allow_http | bool }}"
    security_firewall_effective_https: "{{ ovpn_firewall_allow_https | bool }}"

- name: Build effective public service port sets
  ansible.builtin.set_fact:
    security_firewall_effective_tcp_ports: >-
      {{
        (
          security_firewall_effective_ssh_ports
          + ([80] if security_firewall_effective_http else [])
          + ([443] if security_firewall_effective_https else [])
          + (ovpn_firewall_extra_tcp_ports | default([]) | map('int') | list)
        ) | unique | sort
      }}
    security_firewall_effective_udp_ports: "{{ (ovpn_firewall_extra_udp_ports | default([]) | map('int') | list) | unique | sort }}"

- name: Assert fixed SSH policy (port 22 only)
  ansible.builtin.assert:
    that:
      - (ovpn_ssh_port | default(22) | int) == 22
      - (ovpn_firewall_ssh_port | default(22) | int) == 22
      - (ovpn_firewall_additional_ssh_ports | default([]) | length) == 0
      - not (ovpn_disable_default_ssh_port | default(false) | bool)
      - not (ovpn_confirm_disable_default_ssh_port | default(false) | bool)
    fail_msg: "SSH port migration is removed from this project. Keep SSH on port 22."

- name: Configure unattended security upgrades (optional)
  when: ovpn_enable_unattended_upgrades | bool
  block:
    - name: Enable unattended upgrades package config
      ansible.builtin.copy:
        dest: /etc/apt/apt.conf.d/20auto-upgrades
        owner: root
        group: root
        mode: "0644"
        content: |
          APT::Periodic::Update-Package-Lists "1";
          APT::Periodic::Download-Upgradeable-Packages "1";
          APT::Periodic::AutocleanInterval "7";
          APT::Periodic::Unattended-Upgrade "1";

    - name: Install ovpn unattended upgrades policy
      ansible.builtin.template:
        src: unattended-upgrades-ovpn.j2
        dest: /etc/apt/apt.conf.d/52ovpn-unattended-upgrades
        owner: root
        group: root
        mode: "0644"

    - name: Enable unattended-upgrades service
      ansible.builtin.service:
        name: unattended-upgrades
        state: started
        enabled: true

- name: Disable unattended upgrades package config when unattended upgrades are off
  when: not (ovpn_enable_unattended_upgrades | bool)
  ansible.builtin.copy:
    dest: /etc/apt/apt.conf.d/20auto-upgrades
    owner: root
    group: root
    mode: "0644"
    content: |
      APT::Periodic::Update-Package-Lists "0";
      APT::Periodic::Download-Upgradeable-Packages "0";
      APT::Periodic::AutocleanInterval "7";
      APT::Periodic::Unattended-Upgrade "0";

- name: Remove ovpn unattended upgrades policy when unattended upgrades are off
  when: not (ovpn_enable_unattended_upgrades | bool)
  ansible.builtin.file:
    path: /etc/apt/apt.conf.d/52ovpn-unattended-upgrades
    state: absent

- name: Remove declared apt source files (optional)
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop: "{{ ovpn_remove_apt_source_files | default([]) }}"
  register: security_removed_apt_sources

- name: Refresh apt cache after apt source cleanup
  ansible.builtin.apt:
    update_cache: true
  when:
    - not ansible_check_mode
    - security_removed_apt_sources is changed

- name: Purge declared packages (optional)
  ansible.builtin.apt:
    name: "{{ ovpn_purge_packages | default([]) }}"
    state: absent
    purge: true
    autoremove: true
  when: (ovpn_purge_packages | default([]) | length) > 0

- name: Check sensitive runtime files
  ansible.builtin.stat:
    path: "{{ item.path }}"
  loop:
    - path: "{{ ovpn_deploy_root }}/.env"
      owner: root
      group: root
      mode: "0600"
    - path: "{{ ovpn_xray_config_path }}"
      owner: root
      group: "65532"
      mode: "0640"
  register: security_sensitive_runtime_files
  loop_control:
    label: "{{ item.path }}"

- name: Lock down existing sensitive runtime files
  ansible.builtin.file:
    path: "{{ item.stat.path }}"
    owner: "{{ item.item.owner }}"
    group: "{{ item.item.group }}"
    mode: "{{ item.item.mode }}"
  loop: "{{ security_sensitive_runtime_files.results }}"
  loop_control:
    label: "{{ item.stat.path }}"
  when: item.stat.exists

- name: Check backup root
  ansible.builtin.stat:
    path: "{{ ovpn_backup_root }}"
  register: security_backup_root

- name: Lock down backup root
  ansible.builtin.file:
    path: "{{ ovpn_backup_root }}"
    owner: root
    group: root
    mode: "0750"
  when: security_backup_root.stat.exists

- name: Find existing backup archives
  ansible.builtin.find:
    paths: "{{ ovpn_backup_root }}"
    patterns: "*.tgz"
    file_type: file
  register: security_backup_archives
  when: security_backup_root.stat.exists

- name: Lock down existing backup archives
  ansible.builtin.file:
    path: "{{ item.path }}"
    owner: root
    group: root
    mode: "0600"
  loop: "{{ security_backup_archives.files | default([]) }}"
  loop_control:
    label: "{{ item.path }}"

- name: Configure journald disk limits
  block:
    - name: Ensure journald drop-in directory exists
      ansible.builtin.file:
        path: /etc/systemd/journald.conf.d
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Install ovpn journald limits
      ansible.builtin.template:
        src: journald-ovpn.conf.j2
        dest: /etc/systemd/journald.conf.d/60-ovpn.conf
        owner: root
        group: root
        mode: "0644"
      notify: Restart systemd-journald

    - name: Vacuum journald archives by size target
      ansible.builtin.command: "journalctl --vacuum-size={{ ovpn_journald_system_max_use }}"
      changed_when: false
      when: not ansible_check_mode

- name: Apply swapfile baseline
  ansible.builtin.import_tasks: swapfile.yml

- name: Configure conntrack limits for VPN/NAT workloads
  when: ovpn_manage_conntrack | default(true) | bool
  block:
    - name: Validate conntrack limits
      ansible.builtin.assert:
        that:
          - (ovpn_conntrack_max | default(65536) | int) >= 8192
          - (ovpn_conntrack_tcp_timeout_established | default(86400) | int) >= 3600
        fail_msg: "Keep ovpn_conntrack_max >= 8192 and ovpn_conntrack_tcp_timeout_established >= 3600."

    - name: Ensure nf_conntrack module is loaded
      ansible.builtin.command: modprobe nf_conntrack
      changed_when: false
      when: not ansible_check_mode

    - name: Persist nf_conntrack module loading
      ansible.builtin.copy:
        dest: /etc/modules-load.d/98-ovpn-conntrack.conf
        owner: root
        group: root
        mode: "0644"
        content: |
          nf_conntrack

    - name: Persist conntrack sysctl settings
      ansible.builtin.copy:
        dest: /etc/sysctl.d/98-ovpn-conntrack.conf
        owner: root
        group: root
        mode: "0644"
        content: |
          net.netfilter.nf_conntrack_max = {{ ovpn_conntrack_max | default(65536) | int }}
          net.netfilter.nf_conntrack_tcp_timeout_established = {{ ovpn_conntrack_tcp_timeout_established | default(86400) | int }}

    - name: Apply conntrack sysctl settings
      ansible.builtin.command: sysctl --system
      changed_when: false
      when: not ansible_check_mode

    - name: Ensure node-exporter textfile directory exists
      ansible.builtin.file:
        path: "{{ ovpn_node_exporter_textfile_dir | default('/var/lib/ovpn-node-exporter-textfile') }}"
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Install conntrack metrics writer
      ansible.builtin.template:
        src: ovpn-conntrack-metrics.sh.j2
        dest: /usr/local/sbin/ovpn-conntrack-metrics.sh
        owner: root
        group: root
        mode: "0755"

    - name: Install conntrack metrics service
      ansible.builtin.template:
        src: ovpn-conntrack-metrics.service.j2
        dest: /etc/systemd/system/ovpn-conntrack-metrics.service
        owner: root
        group: root
        mode: "0644"

    - name: Install conntrack metrics timer
      ansible.builtin.template:
        src: ovpn-conntrack-metrics.timer.j2
        dest: /etc/systemd/system/ovpn-conntrack-metrics.timer
        owner: root
        group: root
        mode: "0644"

    - name: Flush systemd before conntrack metrics timer operations
      ansible.builtin.systemd:
        daemon_reload: true
      when: not ansible_check_mode

    - name: Write initial conntrack metrics
      ansible.builtin.command: /usr/local/sbin/ovpn-conntrack-metrics.sh
      changed_when: false
      when: not ansible_check_mode

    - name: Enable and start conntrack metrics timer
      ansible.builtin.systemd:
        name: ovpn-conntrack-metrics.timer
        state: started
        enabled: true
      when: not ansible_check_mode

- name: Configure UFW firewall (optional)
  when: ovpn_manage_firewall | bool
  block:
    - name: Keep UFW forwarding compatible with Docker bridge networking
      ansible.builtin.lineinfile:
        path: /etc/default/ufw
        regexp: '^DEFAULT_FORWARD_POLICY='
        line: 'DEFAULT_FORWARD_POLICY="ACCEPT"'
        create: false
      notify: Reload ufw

    - name: Set UFW default deny incoming
      ansible.builtin.command: ufw --force default deny incoming
      register: security_ufw_default_in
      changed_when: false

    - name: Set UFW default allow outgoing
      ansible.builtin.command: ufw --force default allow outgoing
      register: security_ufw_default_out
      changed_when: false

    - name: Allow effective TCP ports
      ansible.builtin.command: "ufw allow {{ item }}/tcp"
      loop: "{{ security_firewall_effective_tcp_ports }}"
      loop_control:
        label: "{{ item }}/tcp"
      register: security_ufw_allow_tcp
      changed_when: "'Skipping' not in (security_ufw_allow_tcp.stdout | default(''))"

    - name: Allow effective UDP ports
      ansible.builtin.command: "ufw allow {{ item }}/udp"
      loop: "{{ security_firewall_effective_udp_ports }}"
      loop_control:
        label: "{{ item }}/udp"
      register: security_ufw_allow_udp
      changed_when: "'Skipping' not in (security_ufw_allow_udp.stdout | default(''))"

    - name: Remove obsolete TCP port allows
      ansible.builtin.command: "ufw delete allow {{ item }}/tcp"
      loop: "{{ ovpn_firewall_remove_tcp_ports | default([]) | map('int') | list }}"
      loop_control:
        label: "{{ item }}/tcp"
      register: security_ufw_remove_tcp
      changed_when: "'Rule deleted' in (security_ufw_remove_tcp.stdout | default(''))"
      failed_when: false

    - name: Enable UFW firewall
      ansible.builtin.command: ufw --force enable
      register: security_ufw_enable
      changed_when: false

- name: Configure SSH hardening drop-in
  when: ovpn_harden_ssh | bool
  block:
    - name: Ensure sshd config drop-in directory exists
      ansible.builtin.file:
        path: /etc/ssh/sshd_config.d
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Determine PermitRootLogin mode
      ansible.builtin.set_fact:
        security_ssh_permit_root_login_mode: >-
          {{
            'no'
            if not (ovpn_ssh_permit_root_login | bool)
            else (
              'prohibit-password'
              if (ovpn_ssh_pubkey_auth_only | bool)
              else 'yes'
            )
          }}

    - name: Install ovpn sshd hardening drop-in
      ansible.builtin.template:
        src: sshd-ovpn-hardening.conf.j2
        dest: /etc/ssh/sshd_config.d/50-ovpn-hardening.conf
        owner: root
        group: root
        mode: "0644"
      notify: Reload sshd

    - name: Validate SSH daemon config
      ansible.builtin.command: sshd -t
      changed_when: false

    - name: Flush handlers before connectivity checks
      ansible.builtin.meta: flush_handlers

    - name: Validate SSH ports are reachable from controller
      ansible.builtin.wait_for:
        host: "{{ ansible_host | default(inventory_hostname) }}"
        port: "{{ item }}"
        state: started
        timeout: 20
        connect_timeout: 3
      loop: "{{ security_ssh_effective_ports }}"
      when: ovpn_validate_ssh_ports | bool
      delegate_to: localhost
      become: false

- name: Install and configure fail2ban (optional)
  when: ovpn_use_fail2ban | bool
  block:
    - name: Install fail2ban
      ansible.builtin.apt:
        name: fail2ban
        state: present
        update_cache: true

    - name: Deploy ovpn fail2ban jail configuration
      ansible.builtin.template:
        src: jail-ovpn.local.j2
        dest: /etc/fail2ban/jail.d/ovpn.local
        owner: root
        group: root
        mode: "0644"
      notify: Restart fail2ban

    - name: Ensure fail2ban service is running
      ansible.builtin.service:
        name: fail2ban
        state: started
        enabled: true

- name: Enable Tor exit blocking profile (optional)
  when: ovpn_block_tor_exit_nodes | bool
  block:
    - name: Install Tor exit blocking dependencies
      ansible.builtin.apt:
        name:
          - ipset
        state: present
        update_cache: true

    - name: Install Tor exit updater script
      ansible.builtin.template:
        src: ovpn-tor-exit-update.sh.j2
        dest: /usr/local/sbin/ovpn-tor-exit-update.sh
        owner: root
        group: root
        mode: "0755"

    - name: Install Tor exit updater service unit
      ansible.builtin.template:
        src: ovpn-tor-exit-update.service.j2
        dest: /etc/systemd/system/ovpn-tor-exit-update.service
        owner: root
        group: root
        mode: "0644"
      notify: Reload systemd daemon

    - name: Install Tor exit updater timer unit
      ansible.builtin.template:
        src: ovpn-tor-exit-update.timer.j2
        dest: /etc/systemd/system/ovpn-tor-exit-update.timer
        owner: root
        group: root
        mode: "0644"
      notify: Reload systemd daemon

    - name: Flush handlers before systemd operations
      ansible.builtin.meta: flush_handlers

    - name: Run Tor exit updater now (fail-open)
      ansible.builtin.command: /usr/local/sbin/ovpn-tor-exit-update.sh
      when: not ansible_check_mode
      changed_when: false

    - name: Enable and start Tor exit updater timer
      ansible.builtin.systemd:
        name: ovpn-tor-exit-update.timer
        enabled: true
        state: started
      when: not ansible_check_mode

- name: Disable Tor exit blocking profile when turned off
  when: not (ovpn_block_tor_exit_nodes | bool)
  block:
    - name: Stop Tor exit updater timer
      ansible.builtin.systemd:
        name: ovpn-tor-exit-update.timer
        enabled: false
        state: stopped
      failed_when: false

    - name: Stop Tor exit updater service
      ansible.builtin.systemd:
        name: ovpn-tor-exit-update.service
        enabled: false
        state: stopped
      failed_when: false

    - name: Remove Tor exit iptables drop rule
      ansible.builtin.command: >-
        iptables -D INPUT -p tcp --dport {{ ovpn_tor_exit_block_port | int }}
        -m set --match-set ovpn_tor_exit src -j DROP
      register: security_tor_rule_remove
      failed_when: security_tor_rule_remove.rc not in [0, 1, 2, 127]
      changed_when: security_tor_rule_remove.rc == 0

    - name: Remove Tor exit ipset
      ansible.builtin.shell: |
        if ! command -v ipset >/dev/null 2>&1; then
          exit 0
        fi
        ipset destroy ovpn_tor_exit
      register: security_tor_set_remove
      failed_when: security_tor_set_remove.rc not in [0, 1]
      changed_when: security_tor_set_remove.rc == 0

    - name: Remove temporary Tor exit ipset
      ansible.builtin.shell: |
        if ! command -v ipset >/dev/null 2>&1; then
          exit 0
        fi
        ipset destroy ovpn_tor_exit_tmp
      register: security_tor_tmp_set_remove
      failed_when: security_tor_tmp_set_remove.rc not in [0, 1]
      changed_when: security_tor_tmp_set_remove.rc == 0

    - name: Remove Tor exit updater script
      ansible.builtin.file:
        path: /usr/local/sbin/ovpn-tor-exit-update.sh
        state: absent

    - name: Remove Tor exit updater service unit
      ansible.builtin.file:
        path: /etc/systemd/system/ovpn-tor-exit-update.service
        state: absent
      notify: Reload systemd daemon

    - name: Remove Tor exit updater timer unit
      ansible.builtin.file:
        path: /etc/systemd/system/ovpn-tor-exit-update.timer
        state: absent
      notify: Reload systemd daemon
